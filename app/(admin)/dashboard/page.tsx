import DashboardClient from '@/components/DashboardClient';
import './dashboard.css';

export const metadata = { title: 'ASES — Dashboard' };

export default function DashboardPage() {
  return <DashboardClient />;
}
